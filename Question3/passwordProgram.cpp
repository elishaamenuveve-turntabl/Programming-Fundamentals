#include <iostream>

using namespace std;

int main() {
    /*Write a program to prompt for three parts of a password. 
    The program should copy the the first part into a holding character array, 
    and then concatenate the remaining parts to the end of this array. 
    The password array should then be converted to upper case, 
    and compared with the master password (which may be stored as a string literal) HASTA LA VISTA. 
    If the password matches allow your user to have a coffee; if it fails, request that they try again. 
    This program should make use of your mystring library. */

    return 0;
}